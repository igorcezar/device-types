apiVersion: v1
kind: Secret
metadata:
  name: statistics-api-secrets
  labels:
    {{- include "statistics-api.labels" . | nindent 4 }}
type: Opaque
stringData:
  # Database credentials - replace with actual sealed secrets in production
  DB_HOST: "postgres"
  DB_PORT: "5432"
  DB_NAME: "statistics"
  DB_USER: "statistics_user"
  DB_PASSWORD: "CHANGE_ME_IN_PRODUCTION"
